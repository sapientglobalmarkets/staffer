'use strict';

module.exports = function() {

    var config = {
        SRC_DIR: './src',
        DIST_DIR: './dist',
        TYPINGS_DIR: './typings',
        DEFAULT_PORT: '8080'
    };

    return config;
};
