# Generate Data for Staffer

This application generates insert statements to populate the staffer schema. Here's how to run it:

```bash
$ npm install
$ npm run compile
$ npm start
```
This will generate SQL insert statements in load-data.sql.